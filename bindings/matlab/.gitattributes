# mwrap output, generated from simdrng.mw (see CMakeLists.txt). Committed because
# it is platform-independent; marked generated so it collapses in diffs and drops
# out of GitHub blame / PR review noise.
generated/** linguist-generated=true -diff
